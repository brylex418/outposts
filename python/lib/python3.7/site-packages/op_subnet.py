import boto3
import botocore

ec2 = boto3.client('ec2')

def createAndTagSubnet(az, vpc, cidr, oparn, name, rt):
    opsubnet = ec2.create_subnet(AvailabilityZone=az,VpcId=vpc,CidrBlock=cidr,OutpostArn=oparn)
    subnetId = (opsubnet['Subnet']['SubnetId'])
    tag = ec2.create_tags(
        Resources=[
            subnetId,
            ], 
        Tags=[
            {
                'Key': 'Name',
                'Value': name
            },
            ]
        )
    associateroute = ec2.associate_route_table(RouteTableId=rt, SubnetId=subnetId)
    return subnetId
    
def deleteSubnet(subnetId):
    ec2.delete_subnet(
        subnetId=subnetId,
    )